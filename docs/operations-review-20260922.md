# 运营与审核双入口（2026-09-22）

最新访问规则：用户明确要求“不要访问凭证，每一台电脑都可以访问”。运营查看和内部处理标记现已公开，无需登录、访问链接凭证或本机授权。移除登录界面与浏览器凭证验证，本机入口改为普通网址。保留同站请求校验、记录版本检查及服务端数据同步凭证；公众不能上传或覆盖店铺数据快照。下文首次部署的访问授权描述为历史记录，已被本条替代。

用户授权：本机退款、零库存增加“已经处理”，并部署至现有 Vercel 审核站，将运营与审核做成两个工作区。

- 本机运营仍为 http://127.0.0.1:8768/；退款与零库存支持标记、撤销、处理状态筛选。
- https://flowhub-review.vercel.app/ 保留原审核；/operations/ 是运营。首次从本机左侧“云端运营”入口授权浏览器。访问凭证与审核同步凭证分离，服务端通过 HMAC 派生；浏览器只取得运营权限，不取得店铺密钥或审核同步权限。
- 云端仅同步退款/售后和未归档零库存，不上传买家消息或店铺 API 密钥。买家消息仍在本机工作台处理。
- 云端处理记录为权威状态；本机约每分钟拉取标记，写入先由云端确认。网络失败不显示为处理成功。记录有版本校验，隔离不同店铺、履约类型与记录 ID，并保存操作时间与审计历史。
- 平台售后状态与内部“已经处理”独立。标记不会退款、改库存、上下架或发送消息。零库存解除后再次出现会重新进入待处理。
- 平台读取周期保留：售后约 10 分钟，库存约 15 分钟；云端扫描周期 60 秒，仅变化时上传，未变化最多每 5 分钟刷新镜像时间。本机离线保留旧快照并显示时间/延迟。

## 部署边界与验证

从主基线 99abf34 创建隔离 worktree `FlowHub-ops-review`，分支 `codex/operations-review-20260922`。复用既有 `FlowHub-operations` 已运行代码，变更仅包括运营标记、独立同步及审核站导航。没有改动主 FlowHub 业务代码、数据库、发布队列或主服务；基线校验通过。

Vercel 仍为原 `liyansihaos-projects/flowhub-review` 项目、原 Neon 数据库。新增键均以 `operations:` 为前缀，独立写锁，不改审核数据表与审核写入逻辑。

本机独立服务受控发布至 `~/Library/Application Support/Ozon Operations/releases/20260922-handling-2`，仅重启 `com.ozon.operations`。发布前确认发送中消息为 0，备份本机运营 SQLite 与 LaunchAgent；证据在 `output/deployment-20260922*`。中文/俄文 curl 传输已由真实本地 HTTP 回显测试覆盖。

验证：18 项 Python 运营测试、5 项 Node 审核/运营测试；浏览器演示环境验证退款与库存标记、筛选、刷新持久化、撤销；云端页面通过浏览器拦截的测试数据验证标记与撤销，未写入真实业务处理标记。真实只读回查 27 店、62 售后、833 零库存（核验时快照，并非固定数量）；现有审核 API 正常。截图在 `output/playwright/`。

## 回退

### 原因与订单阶段补充（同日）

新增只读 `/v2/returns/rfbs/get` 与 `/v3/posting/fbs/get`，详情缓存 1 小时，读取失败 10 分钟重试。保留平台原因原文，使用逐项中文释义，分别展示原因来源、订单取消原因、订单阶段、核验依据和时间。故障记录显示待核实，不根据处置/退款状态猜测签收。特别是 `fact_delivery_date` 在取消订单上也出现，不能单独作为签收证据。

本次 62 条均取得原因：按核验规则 16 条交付时拒收、12 条订单状态 delivered、31 条确认运输/发货后取消但签收未确认、3 条阶段待核实。5 条订单详情读取失败，其中 2 条可通过明确拒收原因分类。平台原因“未按时发货”共 21 条，不将此原因自动转换为发货前取消，因为关联订单可能明确标记 cancelled_after_ship=true。原因描述不代表卖家责任认定。

部署：独立运营 `20260922-reasons-1`；Vercel `dpl_324Cqcm9GaQSzKoNqcsFqJZpbN6M`。核验与备份 `output/deployment-reasons-20260922`。20 项运营测试、5 项云端/审核测试通过。原平台列表同步时间在补充原因时保留，未冒充重新采集。

只恢复独立运营服务上一版 LaunchAgent 的 WorkingDirectory，并重启其服务。保留现有运营 SQLite、cloud.json 及云端 `operations:` 记录，不恢复旧数据库覆盖新标记。Vercel 可以回到前一审核部署；新记录保留，不删除审核数据。不要重启退役发布器或改动主生产基线。
