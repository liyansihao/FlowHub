# 节流第一阶段试运行

2026-09-16 10:34:05 至 11:04:05（北京时间），原生产 2500ms 请求间隔试调至 2000ms。

实现位于 ozon-runtime/lib/maozi-transport.mjs。共享账本旁的 .trial.json 控制试运行，所有使用该生产 transport 的执行器在原账本锁内检查配置。保留既有预约槽位和 FBS 间隔，成功请求逐步降低 adaptive 值，不直接重写账本。

新的 429 使累计计数偏离启动基线，永久退出本次提速；网络错误或 HTTP 5xx 设置退出标志；到期或配置无效恢复 2500ms 基线。原有 Retry-After 和更慢的自适应退避优先。未知写入不重发。旧版独立直连实现不读取试运行配置，但仍共享原账本；没有修改它们的独立间隔。

验证：15 项 transport 测试通过。工作进程已安全重启为 44278，三个模块未暂停。部署后观察预约间隔从 2500→2250→2025→2000ms，连续多个 2000ms 槽位；累计限流仍 137，连续限流 0，无试运行失败标志。

本记录仅证明配置生效，尚不能证明成功上架吞吐量提升。试运行结束后需按首次库存确认统计同长窗口，再决定下一档；不会自动降至 1500ms。
