# SQLite 修复代码检查点（2026-09-22）

本检查点按用户授权，在外部 ERP/Ozon 故障与 SQLite 修复分开评价后合入 main。
它表示修复已通过代码检查、完整自动化测试和真实数据副本回放；不表示连续 10 小时生产验收已通过。

## 合入范围

基于 main `4679b518c6a2ccd84cc09d62bdcae57aff306661`，只移植已在生产运行的
`934fcfd`、`3348e28`、`8d8c86f`、`a62f930`、`2cb57e0`、`4777cc6` 的 SQLite 修复：

- 来源商品与游标检查点、失败记录及补资料检查点在后台线程执行完整事务，取消时等待事务结束。
- 准入先排序商品主键，再在同一事务内加载正文；部分覆盖索引避免筛选时反复读取正文。
- 续期一次连接队列表筛除终态，保留 NULL、历史授权及未决写入语义。
- 卖家查询索引与未改变评估结果的写入去重。

main 已有的 schema_once/WAL 初始化缓存保持不变。补资料模块沿用 main 原有结构，
仅提取最终完整检查点；不带入生产分支的阶段拆分、可选资料门禁或其他业务功能。
`database_work.py` 是上述异步检查点的必要运行依赖，不是临时诊断。

未合入临时 SQLite 诊断模块、环境开关、日志输出、验收脚本和数据库副本；
未合入已回退的 `47452f5`/`3e6ee09`、未验证的 `9b19d2c` 或生产目录的 12 个本地修改。
未改发布后端、选品规则、库存、人工决定、外部接口策略或事务边界。

## 验证

- 完整 Python 测试：730 passed（2 条依赖弃用提醒）。
- 完整 Node 测试：24 passed。
- 包含真实 SQLite 锁等待、取消后事务结束、补资料日志/商品原子回滚、准入顺序、续期 NULL/终态和评估幂等回归。
- 已有生产快照 14,975 个来源产品、2,127 个准入候选，升/降序与有/无优先队列四组合只读回放，与原 main 候选身份和顺序完全相同。
- 开发工作树测试使用独立 data 目录；FlowEF 与 ozon-runtime 仅作为既有依赖加载，没有启动它们的发布服务。

## 生产与后续验收

本次 Git 合并不是生产切换。唯一生产目录仍运行 `99abf347f3a390c1b110ca2874615984dc2f266a`，
源码指纹 `92b800964f15fef9fd3201071111da0d704699877a16c2e69645c0a56684e070`，
12 个本地修改、模型缓存、账本及未知写入均保留。
生产分支还有不属于本次 SQLite 修复的功能；不能把新 main 直接覆盖到生产目录。

合并前最新只读采样未见新的 SQLite 锁错误或 worker 重启。外部接口仍有超时；
另有两次约 5–6 秒事件循环延迟，未证明全部长事务风险已经消失。
单商品 review RuntimeError 后已进入 rejected，不能宣称该次审核成功。

停止继续美化或重构 SQLite。外部服务恢复后，先将既有生产功能/12 个本地修改安全对齐到新 main 的候选，
完成相关测试和受控部署记录，再重新开始完整长期验收。数据、历史意图和隔离项不能重置；
只计真实首次 stock_verified 且 verified=true/backend=maozi_follow，明确新入组与未完成分母。
外部故障独立归因，不用它认定 SQLite 修复失败，也不把受干扰窗口认证为完整验收通过。

## 回滚

修复标签：`stable/sqlite-fix-20260922`。
原 main 标签：`checkpoint/before-sqlite-stable-main-20260922`。
标签固定代码，可从标签创建隔离恢复分支。生产回退仍须核对部署清单和本地补丁，受控切换唯一服务；
不得用 git reset 或数据库备份覆盖活跃数据、删除账本，或启动任何旧发布器。
本机证据：`output/sqlite-stability-acceptance-20260921/main-merge-*`（位于 ozon 根目录）。
