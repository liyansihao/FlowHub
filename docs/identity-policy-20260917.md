# 2026-09-17 同款审核分流修复

用户要求网页按 DINO／千问机制分流。此前独立复核固定返回 needs_review，已有 approved 高分小商品也会重复要求人工审核。

现在 DINO < 0.63 淘汰，DINO >= 0.86 且 query.size=small 通过，其余使用千问；同款且 >= 0.82 通过，不同款且 <= 0.64 淘汰，其他保留人工审核。沿用原有千问明确品牌／型号冲突淘汰保护。无候选、无有效分数不算不同款。小商品继续沿用重量 < 500g 且人民币售价 < 135 的定义；不把卢布直接当人民币，缺少判定资料交千问。

独立复核返回 same_product_confirmed / rejected / needs_review，报告状态同步落库。进入人工队列前也复用绑定正确的确定结论，避免旧流程再次产生高分待审。通过同款审核只改变审核状态；上架仍由网站操作授权，自动审核记录不伪装成人工审核。

scripts/apply_identity_policy.py 默认只预览。--apply --backup PATH 在同一事务内重分流现有待审核记录，私密备份保留原报告和队列；跳过人工决定、强制重审、变更输入、发布中、受阻和活动处理记录。不调用上下架接口。未完成千问的有效中间候选转回 review 队列。

上线验证：后台 worker 已重载，Vercel 生产域名 flowhub-review.vercel.app 已更新。存量事务重分流 5 件通过、1 件淘汰；SKU 4671364048、4671428401 因后续补充规格与旧记录不同，复用排序重新送千问，两件均得到 qwen_match_in_safe_band 并变为 same_product_confirmed。远端商品快照已回查为 match。迁移和重新识别未创建上下架命令。

验证：114 项相关 Python 测试通过，后增加的旧流程入队回归测试及同文件 31 项测试通过；18 项网页 API／轮询测试通过；Python 编译和 JS 语法检查通过。
