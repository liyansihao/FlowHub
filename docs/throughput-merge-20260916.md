# 资料复用与发布优先级：独立合并记录

## 范围

本提交从 `codex/throughput-reuse-20260916` 生产工作区提取，只包含：

- 远程请求前复用按用户、ERP 账号、SKU 隔离的完整本地草稿；六小时有效期、单变体检查和原始采集时间不变。
- 已审核、等待发布资料的到期任务优先处理；继续遵守 due、暂停、租约和处理名额限制。
- 相同参数 GET 合并，草稿采集换算汇率缓存 60 秒；连接失败后的接口退避按账号隔离。写入不自动重试。
- 独立测量脚本及回归测试。

缓存复用所需的最小兼容改动：`merge_fields` 和 `merge_draft` 接受可选 `now`，以当前时刻判断有效性、避免旧快照覆盖较新字段；不更改原采集时间。生产工作区原有的全资料参数、资料解析、人工审核、集群、历史回查等修改未随本次合并提交。

## 实测记录与限制

| 指标 | 修改前 | 修改后 |
|---|---:|---:|
| 15 分钟首次确认可售 | 6 | 8 |
| 15 分钟审核到可售中位耗时 | 9.13 分钟 | 13.73 分钟 |
| 昨晚20—24点 / 今天1—8点确认可售 | 100 / 4小时 | 267 / 7小时 |
| 对应平均每小时 | 25 | 38.14 |
| 对应审核到可售中位耗时 | 22.25 分钟 | 10.04 分钟 |

按每个发布记录第一次进入 stock_verified 统计，不把提交接受或重复回查算新增可售。数据反映整个生产工作区当时的运行效果，不是这个独立提交的随机对照实验。不同商品批次、浏览器扩采关闭、进程稳定程度和上游网络均影响结果。整晚本地草稿优先复用命中为 0，不能宣称已实测出这一项收益。错误事件包含本地主动退避，不能等同于失败商品数。

原始本地记录（不包含在 Git 提交中）：`reports/throughput-reuse-20260916/` 下的 baseline-window.json、after-window.json、overnight-comparison.json、deployment.json、completion.json。合并前后校验记录在 `reports/throughput-merge-20260916/`。

## 验证

在 main 的独立干净检出中验证，不借用生产工作区未提交的 Python 模块。使用仓库原有外部依赖 FlowEF-production：

```sh
PYTHONPATH=/Users/mac/Desktop/ozon/FlowEF-production/src /Users/mac/Desktop/ozon/FlowHub/.venv/bin/python -m pytest -q
```

本轮独立定向测试先通过 73 项。完整 Python 测试通过 **399 项**，只有两条依赖弃用提示，无测试失败。

## 合并及回退

合并只改变 Git 提交和分支指向，不覆盖生产工作区已有文件，不重启服务，不修改数据库或运行配置，不自动恢复浏览器扩采。

合并前基线为 `d4b077c`。要撤销本提交，在干净检出中对本次提速提交执行 `git revert <本次提交ID>`，先验证并审阅反向差异，再按现有排空流程部署。实际提交ID与工作区文件校验保存在本地合并记录。不要用 reset --hard 或恢复整库回退；生产工作区仍含其他未提交功能，不能将干净 main 文件直接整批覆盖到运行目录。
