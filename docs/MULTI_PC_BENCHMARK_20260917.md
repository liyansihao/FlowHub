# 三台 Windows 扩容验证与连接故障隔离

2026-09-17 的完整结果见 `output/multi-pc-test-20260917/REPORT.md`。

固定 9 个图片任务（模型常驻、重复输入、缓存已热）：Windows-04 单机 60.68 秒；04+02 双机 40.84 秒；04+02+03 三机 33.29 秒。27 次成功，三机计算吞吐为该单机样本的约 1.82 倍，不能解释为上架速度倍数。

当时发布积压阻断新审核，且 Mac 发往 ERP 的连接存在约 10.5 秒超时；Windows-04 对同一导入日志查询成功。生产并发对照受网络故障、积压与不同商品阶段影响，没有证明最终可售吞吐随机器数增长。

## 本次修复

- 协调库使用 WAL，避免状态读取阻塞设备心跳和结果提交，保留 FULL 同步与原事务边界。
- 连接故障冷却按执行电脑隔离。`StepTransport` 保留账号范围的缓存、合并请求与失效逻辑，只有网络故障冷却键增加 `circuit_namespace`。Windows 路由标注设备 ID，发布步骤将它传入传输层。
- 不改变账号请求节奏、不重试未知写入、不降低审批或库存校验。

## 验证与限制

24 项协调器/计算/ERP 测试通过，随后 56 项发布调度与相关多机保护测试通过（两组有重叠，不应累加）。新增用例覆盖读取旧快照同时完成任务，以及 Mac 连接失败不会冷却健康 Windows、Windows 成功也不会清除 Mac 的故障冷却。

第一轮诊断脚本保留游标导致读锁，受影响的三机结果作废；修复诊断脚本并迁移 WAL 后完整重测计算批次。生产观测为短时验证，尚未完成 24 小时稳定运行测试。库存确认不等于最终可售。

## 运行配置与回退

计算白名单三台、额外审核通道两个；ERP 白名单三台，优先 04、02、03。原配置备份位于上述 output 目录，协调库备份为 `data/cluster/multi-pc-before-wal.sqlite3`，仅供恢复审计，不能直接覆盖正在运行且已接收新结果的数据库。

回退应先排空任务，恢复相关代码及对应配置；WAL 可以保留。任何数据库回退必须先停止协调器并评估备份后新增命令，不能丢弃未知写入记录。主控所有模块原暂停状态应保留。
