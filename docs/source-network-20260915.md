# 2026-09-15 来源浏览器网络恢复

原专用 profile 仍保留在 `data/source-loop.json`。系统代理为本机 7897，Ozon 请求经当前新加坡线路时出现超时及 `net::ERR_FAILED`；普通站点代理请求成功。相同机器原生直连 Ozon 可以收到响应。

使用原 profile、仅为采集浏览器添加 `--no-proxy-server` 后，首页最初返回访问检查页，随后自行进入正常商城首页。保留原有毛子插件，未导入或更改账号凭证。个人账号是否已登录未单独验证，商城可访问不等同账号登录成功。

`bridges/source-network.mjs` 读取当前实例 `source-loop.json` 的 `browser_network`：默认 `system`，设置 `direct` 时只让来源浏览器直连。整店采集和其他卖家发现共用该配置；不修改 macOS 系统代理。配置在每个浏览器进程启动时读取，无需重启发布工作进程。

真实验收：原 `playwright-source.mjs` 从卖家 4233925 第 1 页读取 8 个 SKU，新增入库 8 个，并保存第 2 页游标。一次独立店铺导航仍出现超时，因此此验收不代表所有来源或长期稳定性已通过。

按最新错误仅重新排队网络失败／访问检查失败的 213 个扫描和 5 个其他卖家发现任务；保留原游标、根来源证据、手动暂停、数据校验失败与发布状态。重试前记录在 `output/playwright/network-recovery-20260915/retry-before.json`。该修复不将人工同款确认转为自动发布。

验证：3 个 Node 文件语法检查通过；配置缺省、系统代理、直连、无效值四项检查通过。扩展后台已重新开启。若需恢复系统代理，将 `browser_network` 改回 `system`，下一轮浏览器启动即生效。
