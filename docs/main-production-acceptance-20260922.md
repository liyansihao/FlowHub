# main 全链路生产验收候选

此候选尚未通过长期验收，不是正式稳定版。

用户2026-09-22要求对main进行真实Ozon全链路10–24小时验收。旧main缺少当前生产已使用的ERP跟卖、资料恢复和运行监控，不能直接覆盖生产。
本次以main 6d9fcf7为父提交，逐文件保留当前生产99abf34的现有运行实现、测试和必要部署依赖，将12项已登记本地补丁纳入版本控制。不是整枝合并，不引入新的业务功能、发布/库存规则或试验版本。

对齐阶段162个运行源文件摘要为92b800964f15fef9fd3201071111da0d704699877a16c2e69645c0a56684e070，与现网逐字节相同。完整936项Python、27项Node测试通过。生产文件和补丁归档在ozon根目录output/flowhub-main-stability-20260922/production-alignment.json和before-main-live-source.tar.gz。数据、凭据、日志和验收工具不进入提交。

后续只修复已确认的普通缺陷；任何代码修复受控部署后重新计时。完成至少10小时（目标24小时）连续真实运行，核对每完整滚动小时至少30件真实首次核验，记录新入组分母/成功/失败/未完成、重试、耗时、单品失败隔离、外部故障恢复、worker恢复和锁错误。外部故障独立归因，不自动算作SQLite失败。证据不全不认证，正式v1.0-stable标签仅在完整验收后创建。

## 验收准备发现的缺陷与修复

生产source_seed_resolutions中存在24次仍waiting的任务；原resolve_one每6小时重试却没有终止预算。修复分支codex/seed-resolution-retry-budget-20260922仅给种子身份解析增加首次加3次延迟重试的预算，第四次失败转retry_exhausted；历史已超限记录到期后直接耗尽，不再请求，保留原因/证据/次数和原种子，不改变发布授权或人工决定。此状态不等于种子解析成功，后续只允许明确人工复核后再恢复，不自动清零重发。

实际生产只读提取172条已超限记录，在内存副本回放，0外部请求，原次数/原因/证据/到期均保留。新种子可继续推进，第四次内恢复可正常成功，重启后耗尽状态仍生效。
完整940项Python测试、27项Node测试通过。真实supervisor循环隔离测试中，替身worker以23退出后5秒退避重新拉起，监督进程可正常停止并释放锁；没有故意杀死生产worker或伪造远端写入。长期实际异常恢复仍需持续观察。
